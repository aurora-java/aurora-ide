package aurora.ide.core.server.launch;

import aurora.ide.core.Activator;

public interface ILaunchConstants {
	String DEPLOY_PROJECT = Activator.PLUGIN_ID + ".deploy.project";
	String SERVER_PORT = Activator.PLUGIN_ID + ".server.port";
}
